This application creates a profile for the logged in github user . The data stored in the database can be updated using "update" link . "Show Users" link shows all the users in a paginated manner,clicking on any user redirects to his profile . In his profile, clicking on "public repositories" will redirect to all the page showing all public  repositories . Clicking any of it will show the repository information of that particular clicked repository .